# ───── 필수 런타임 ─────
streamlit          # 웹 앱 프레임워크
pandas             # CSV 처리
numpy              # 수치 연산
matplotlib         # 그래프 (IG·SHAP 시각화)

# ───── 모델 / XAI ─────
torch==2.2.2       # CPU 버전; 3.11 휠 제공
captum==0.7.0      # Integrated Gradients 등
shap==0.45.0       # SHAP 값 계산 (3.11 휠 제공)
